apiVersion: v1
kind: Service
metadata:
  name: {{ include "e5renew.fullname" . }}
  labels:
    {{- include "e5renew.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e5renew.selectorLabels" . | nindent 4 }}
